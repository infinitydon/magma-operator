---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: magmaagws.magma.infra.don
spec:
  group: magma.infra.don
  names:
    kind: MagmaAGW
    listKind: MagmaAGWList
    plural: magmaagws
    singular: magmaagw
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MagmaAGW is the Schema for the magmaagws API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of MagmaAGW
            properties:
              accessGatewayID:
                type: string
              agwNodeLabelSelector:
                additionalProperties:
                  type: string
                type: object
              agwNodeSelector:
                additionalProperties:
                  type: string
                type: object
              datapath:
                description: MagmaAGWDatapathSpec defines AGW host datapath node-prep
                  gating.
                properties:
                  enabled:
                    type: boolean
                  ovsKmodUpgradePath:
                    type: string
                  readyLabelKey:
                    type: string
                  readyLabelValue:
                    type: string
                  requireMagmaOvsKmod:
                    type: boolean
                type: object
              deletionPolicy:
                description: MagmaAGWDeletionPolicySpec defines opt-in cleanup for
                  durable AGW state.
                properties:
                  deleteIdentitySecret:
                    type: boolean
                  deletePVC:
                    type: boolean
                type: object
              enableUERANSIM:
                type: boolean
              gatewayRegistration:
                description: MagmaAGWGatewayRegistrationSpec defines Orc8r/NMS gateway
                  registration intent.
                properties:
                  deleteOnRemoval:
                    type: boolean
                  description:
                    type: string
                  enabled:
                    type: boolean
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              identity:
                description: MagmaAGWIdentitySpec defines the operator-managed AGW
                  bootstrap identity.
                properties:
                  hardwareID:
                    type: string
                  importSecretKey:
                    type: string
                  importSecretName:
                    type: string
                  rotationPolicy:
                    enum:
                    - Never
                    - Rotate
                    type: string
                  secretName:
                    type: string
                type: object
              networkID:
                type: string
              nmsAPIHost:
                type: string
              nmsAdminCertSecretName:
                type: string
              orc8rNamespace:
                type: string
              orc8rReleaseName:
                type: string
              releaseName:
                type: string
              s1Interface:
                type: string
              sgiInterface:
                type: string
              ueransimNodeSelector:
                additionalProperties:
                  type: string
                type: object
              ueransimReadyConfigMap:
                type: string
              ueransimStartPolicy:
                enum:
                - AfterAGWReady
                - Immediate
                type: string
              ueransimValidation:
                description: MagmaAGWUERANSIMValidationSpec defines an optional one-shot
                  simulator validation.
                properties:
                  enabled:
                    type: boolean
                  iperfPort:
                    format: int32
                    type: integer
                  iperfServer:
                    type: string
                  pingHost:
                    type: string
                  timeoutSeconds:
                    format: int32
                    type: integer
                  trigger:
                    type: string
                  ueDeploymentName:
                    type: string
                type: object
              values:
                additionalProperties:
                  type: string
                type: object
            type: object
          status:
            description: status defines the observed state of MagmaAGW
            properties:
              challengePublicKeyHash:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              gatewayRegistered:
                type: boolean
              hardwareID:
                type: string
              identitySecretName:
                type: string
              observedGeneration:
                format: int64
                type: integer
              orc8rServiceIP:
                type: string
              releaseName:
                type: string
              releaseNamespace:
                type: string
              trustBundleHash:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
